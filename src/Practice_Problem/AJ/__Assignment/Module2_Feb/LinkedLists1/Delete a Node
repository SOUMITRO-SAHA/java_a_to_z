/*
Delete a Node
https://course.acciojob.com/idle?question=7b346e14-d70f-4dc0-a82f-f3c2f3da9646
 */
import java.util.*;


public class Main {
    public static void main(String []args) {
        Scanner input = new Scanner(System.in);
        LinkedList ll = new LinkedList();
        int n = input.nextInt();
        for(int i = 0; i < n; i++){
            int x = input.nextInt();
            ll.add(x);
        }
        int toRemove = input.nextInt();
        Solution s = new Solution();
        s.remove(ll, toRemove);
        Node curr = ll.head;
        while(curr != null){
            System.out.print(curr.data + " ");
            curr = curr.next;
        }
    }
}


class Node{
    int data;
    Node next = null;
    Node(int data){
        this.data = data;
        next = null;
    }
}
class LinkedList{
    Node head;
    void add(int data){
        Node new_node = new Node(data);
        if(head == null){
            head = new_node;
            return;
        }
        Node curr = head;
        while(curr.next != null)
            curr = curr.next;
        curr.next = new_node;
    }
}

class Solution {
    public static void remove(LinkedList ll, int index){
        // Getting the Head:
        Node head = ll.head;

        // Reach to the previous Node of target Index:
        Node temp = head;

        // Edge Case: index == 0
        if (index == 0) {
            ll.head = head.next;
            return;
        }
        for (int i = 0; i < index-1; i++) {
            temp = temp.next;
        }
        // Getting the TargetNode
        Node targetNode = temp.next;

        // Connecting the previous node to the next node of targetNode:
        temp.next = targetNode.next;

        targetNode.next = null; // Removing the Target Node

    }
}